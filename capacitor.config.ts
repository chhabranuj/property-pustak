import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.property.pustak',
  appName: 'Property Pustak',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
